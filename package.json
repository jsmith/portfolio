{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "vue-cli-service lint",
    "dev": "vuepress dev jsmith.github.io",
    "build": "vuepress build jsmith.github.io",
    "deploy": "git clone git@github.com:jsmith/jsmith.github.io repo && npm run build && cp -r build/* repo && rm -r build && cd repo && git add . && git commit -m \"release\" && git push && cd .. && rm -rf repo"
  },
  "dependencies": {
    "vue": "^2.5.17",
    "vuepress-theme-terminal": "^0.1.1"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "^3.0.0",
    "@vue/cli-plugin-eslint": "^3.0.0",
    "@vue/cli-service": "^3.0.0",
    "@vue/eslint-config-standard": "^3.0.1",
    "stylus": "^0.54.5",
    "stylus-loader": "^3.0.2",
    "vue-cli-plugin-vuepress": "^0.1.0",
    "vue-template-compiler": "^2.5.17",
    "vuepress": "^0.14.2"
  }
}
